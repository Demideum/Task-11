namespace IJunior_Task11
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int drugIntakeDay;
            
            for (drugIntakeDay = 5; drugIntakeDay <= 96; drugIntakeDay += 7)
            {
                Console.WriteLine(drugIntakeDay);
            }
        }
    }
}
